# Secrets Configuration (example)
# Copy this file to config/secrets.yaml and fill in your keys.
# IMPORTANT: Never commit your real keys. The real file is git-ignored.

alpaca:
  key_id: "YOUR_ALPACA_KEY_ID"
  secret_key: "YOUR_ALPACA_SECRET_KEY"
  base_url: "https://paper-api.alpaca.markets"

# Optional: Other brokers (placeholders)
interactive_brokers:
  enabled: false
  # add your IB credentials here if you integrate IB


